{
  "name": "user-crud-api",
  "version": "1.0.0",
  "description": "",
  "main": "launch.ts",
  "scripts": {
    "server": "json-server --watch src/... AJUSTAR",
    "dev": "ts-node-dev --transpile-only --ignore-watch node_modules --respawn src/launch.ts",
    "inspect": "ts-node-dev --inspect --transpile-only --ignore-watch node_modules --respawn src/launch.ts",
    "jest": "jest --init",
    "test": "jest",
    "test-coverage": "jest --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "pg": "^8.7.3",
    "reflect-metadata": "^0.1.13",
    "typeorm": "^0.3.6"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/jest": "^28.1.4",
    "jest": "^28.1.2",
    "ts-jest": "^28.0.5",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.3"
  }
}
